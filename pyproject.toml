[project]
name = "mist"
version = "0.1.0"
description = "A modular source-to-source code obfuscator."
readme = "README.md"
requires-python = ">=3.11"

authors = [
    { name = "Ananda Rizky" }
]

dependencies = []

[project.scripts]
mist = "mist.cli.main:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"